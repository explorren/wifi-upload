
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/router

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: wiff-injection

server:
  port: 5000




